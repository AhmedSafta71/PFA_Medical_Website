in the project directoy front/ back ,install node modules by :
### `npm init`

In the project front directory, you can run:

### `npm start`

In the project back directory, you can run:

### `nodemon app.js`


